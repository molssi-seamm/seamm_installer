name: release
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - platformdirs
  - pmw
  - requests
  - seamm-widgets
  - tabulate

  # Conda release
  - anaconda-client
  - conda-build

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov
  
  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
