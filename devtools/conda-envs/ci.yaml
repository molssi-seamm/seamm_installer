name: ci
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - pillow
  - platformdirs
  - requests
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov
