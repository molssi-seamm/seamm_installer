name: docs
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - pillow
  - platformdirs
  - requests
  - tabulate
  
  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
