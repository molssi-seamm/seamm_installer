name: test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - requests
    - tabulate

    # Testing
    - yapf

    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
