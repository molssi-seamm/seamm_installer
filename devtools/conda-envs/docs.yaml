name: docs
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - platformdirs
  - pmw
  - requests
  - seamm-widgets
  - tabulate
  
  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
