name: ci
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - platformdirs
  - requests
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - tkhtmlview
