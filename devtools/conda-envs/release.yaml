name: release
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - pillow
  - platformdirs
  - requests
  - tabulate

  # Conda release
  - anaconda-client
  - conda-build

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov
  
  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    - tkhtmlview
    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
