name: ci
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - platformdirs
  - pmw
  - requests
  - seamm-widgets
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov
