name: test
channels:
  - default
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - requests
    - tabulate

      # Testing
    - codecov
    - yapf

