name: docs
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - platformdirs
  - requests
  - tabulate
  
  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    - tkhtmlview
    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
